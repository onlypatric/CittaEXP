integration:
  failFast: true
  ranking:
    scanLimit: 5000
  claim:
    auto:
      width: 100
      height: 100
    # DEPRECATO: da M3 wiring API reale, i comandi non sono piu' usati.
    # Manteniamo la sezione solo per backward-compat di config esistenti.
    commands:
      create: ""
      expand: ""
